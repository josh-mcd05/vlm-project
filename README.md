## Files

image_embedder.py: embeds images, set model and input directories, will output embeddings

retreive_matches.py: uses embeddings to creates matches, set embedding and image directories, will output html of results

server.py: runs server on http://127.0.0.1:5000 to select pairs of images
